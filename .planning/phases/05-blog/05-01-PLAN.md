---
phase: 05-blog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/blog/index.astro
  - src/pages/blog/[...slug].astro
autonomous: true
requirements:
  - BLOG-02
  - BLOG-03
must_haves:
  truths:
    - User can filter blog posts by tag on the listing page
    - Blog posts display code blocks with syntax highlighting
    - Blog posts have proper typography for technical content
  artifacts:
    - path: src/pages/blog/index.astro
      provides: Tag filtering UI with client-side JS
      key_features:
        - Filter buttons for tags
        - JavaScript filtering logic
    - path: src/pages/blog/[...slug].astro
      provides: Enhanced post layout with code highlighting
      key_features:
        - Shiki syntax highlighting (built-in)
        - Better blockquote styling
        - Technical typography refinements
  key_links:
    - from: index.astro
      to: JavaScript
      via: Inline script for filter
    - from: [...slug].astro
      to: Shiki
      via: Astro markdown integration (automatic)
---

<objective>
Complete the blog system by adding tag-based filtering to the listing page and enhancing the post layout with code syntax highlighting for technical geophysics content.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/STRUCTURE.md
@src/pages/blog/index.astro
@src/pages/blog/[...slug].astro
@src/content/config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tag filtering to blog index</name>
  <files>src/pages/blog/index.astro</files>
  <action>
Add client-side tag filtering to the blog index page:

1. Extract all unique tags from posts
2. Add a filter bar with tag buttons before the blog grid
3. Add JavaScript to filter posts by tag when clicked
4. Include "All" button to show all posts
5. Add visual feedback (active state) for selected filter
6. Handle the prefers-reduced-motion accessibility requirement

Implementation notes:
- Extract tags in the frontmatter: `const allTags = [...new Set(posts.flatMap(p => p.data.tags))]`
- Add filter buttons section above `.blog-grid`
- Use data-attributes for filtering: `data-tags` on each card
- Filter logic: show/hide cards based on selected tag match
- Default to show all posts until a filter is clicked
- Maintain accessible keyboard navigation for filter buttons
  </action>
  <verify>
  1. Build succeeds: `npm run build`
  2. Blog page loads at /blog
  3. Tag filter buttons appear above blog cards
  4. Clicking a tag filters the visible posts
  5. "All" button resets the filter
  6. Works without JavaScript disabled (graceful fallback shows all)
  </verify>
  <done>
Tag filter bar appears above blog cards. Clicking "Seismology" shows only seismology-tagged posts. Clicking "All" shows all posts again.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance post layout with syntax highlighting</name>
  <files>src/pages/blog/[...slug].astro</files>
  <action>
Enhance the blog post layout for technical content:

1. Add code block styling using Shiki (Astro includes this)
2. Add blockquote enhancements for citations
3. Add proper styling for lists, tables, and images
4. Ensure responsive typography
5. Add proper spacing between elements

Implementation notes:
- Astro already uses Shiki for code blocks — ensure theme matches site
- In Layout.astro or global.css, add code block base styles
- Use existing `--color-accent` for code highlight accents
- Add styles for: `pre`, `code`, `blockquote`, `ul/ol`, `table`, `img`, `hr`
- Maintain dark theme consistency with existing design

Code block styling approach:
```
.post-content :global(pre) {
  background: rgba(0, 0, 0, 0.3);
  padding: var(--space-lg);
  border-radius: var(--radius-md);
  overflow-x: auto;
  margin: var(--space-xl) 0;
}
.post-content :global(code) {
  font-family: var(--font-mono);
  font-size: 0.9em;
}
```
  </action>
  <verify>
  1. Build succeeds without errors
  2. Navigate to a blog post (/blog/hidup-itu-ujian or /blog/interactive-seismology)
  3. Code blocks have dark background styling
  4. Blockquotes have accent border styling
  5. Typography is readable and consistent
  </verify>
  <done>
Blog posts display code blocks with dark background, proper syntax colors, and readable monospace font. Blockquotes have accent-colored left border.
  </done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Completed blog system with tag filtering and syntax highlighting</what-built>
  <how-to-verify>
1. Visit https://ajamj.github.io/blog
2. Verify tag filter buttons appear above blog cards
3. Click each tag filter — posts should filter correctly
4. Click "All" — all posts should appear
5. Click a blog post — verify code blocks have styling
6. Check that blockquotes have proper styling
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → static | Static site, no user input processing |

## STRIDE Threat Register

This is a read-only static site with no user input. No significant threats.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | Blog posts | Accept | Public portfolio, content is intentionally public |
</threat_model>

<verification>
- [ ] Tag filter buttons render above blog grid
- [ ] Clicking filter buttons updates visible posts
- [ ] Blog post code blocks have styling
- [ ] Build completes without errors
</verification>

<success_criteria>
1. User can filter blog posts by clicking tag buttons
2. All posts display when "All" is selected
3. Code blocks in blog posts have syntax highlighting
4. Site builds and deploys successfully
</success_criteria>

<output>
After completion, create `.planning/phases/05-blog/05-01-SUMMARY.md`
</output>