---
phase: 06-data-viz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/InteractiveSeismogram.astro
  - src/components/ResearchDataViz.astro
  - src/content/blog/interactive-seismology.mdx
autonomous: true
requirements:
  - VIZ-01
  - VIZ-02
  - VIZ-03
must_haves:
  truths:
    - User can view interactive D3.js seismogram visualization on the site
    - Visualization loads external seismic data from JSON/CSV files
    - Visualization supports zoom, pan, and hover interactions
    - Interactive seismogram is embedded and working in a blog post
  artifacts:
    - path: src/components/InteractiveSeismogram.astro
      provides: Interactive D3.js seismogram with real data loading
      key_features:
        - Load seismic data from external JSON file
        - Zoom and pan functionality
        - Hover tooltip showing time and amplitude
        - Responsive design
    - path: public/data/seismic-sample.json
      provides: Sample seismic waveform data
      key_features:
        - JSON format with time-series data
        - Realistic P-wave and S-wave arrivals
    - path: src/components/ResearchDataViz.astro
      provides: Research data visualization section
      key_features:
        - 3 visualization cards with updated data
    - path: src/content/blog/interactive-seismology.mdx
      provides: Blog post with embedded interactive visualization
      key_features:
        - MDX component import works
        - Visualization renders in blog post context
  key_links:
    - from: InteractiveSeismogram.astro
      to: public/data/seismic-sample.json
      via: Fetch API for data loading
    - from: InteractiveSeismogram.astro
      to: D3.js
      via: ES module import in client script
    - from: interactive-seismology.mdx
      to: InteractiveSeismogram.astro
      via: MDX import and component usage
---

<objective>
Complete the interactive data visualization system by ensuring D3.js visualizations work properly, add support for external data files, and verify the blog post integration is functional.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/STRUCTURE.md
@.planning/codebase/STACK.md
@src/components/InteractiveSeismogram.astro
@src/components/ResearchDataViz.astro
@src/content/blog/interactive-seismology.mdx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create external seismic data file</name>
  <files>public/data/seismic-sample.json</files>
  <action>
Create a JSON file containing realistic seismic waveform data that can be loaded by the visualization:

1. Create the directory `public/data/`
2. Create `seismic-sample.json` with time-series data
3. Include realistic P-wave arrival at ~3.0s and S-wave arrival at ~5.5s
4. Include metadata (station, sampling rate, location)
5. Data format:
```json
{
  "metadata": {
    "station": "UGM-01",
    "channel": "SZ",
    "sampling_rate": 100,
    "start_time": "2026-04-17T10:30:00Z",
    "location": {"lat": -7.775, "lon": 110.372}
  },
  "waveform": [
    {"t": 0.00, "amplitude": 0.002},
    {"t": 0.01, "amplitude": -0.001},
    ...
  ]
}
```
  </action>
  <verify>
1. File exists at public/data/seismic-sample.json
2. JSON is valid (parseable)
3. Contains waveform array with at least 1000 data points
4. Contains metadata object
  </verify>
  <done>
public/data/seismic-sample.json exists with valid seismic waveform data.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update InteractiveSeismogram to load external data</name>
  <files>src/components/InteractiveSeismogram.astro</files>
  <action>
Update the InteractiveSeismogram component to fetch and use external data:

1. Modify the script to fetch data from `/data/seismic-sample.json`
2. Add fallback to generated data if fetch fails (for backward compatibility)
3. Add loading state indicator while fetching data
4. Ensure the component works both in regular pages and in MDX blog posts

Implementation approach:
- Use `fetch()` to load JSON data
- Parse the waveform array into the visualization
- Show "Loading..." text while fetching
- Graceful degradation: if fetch fails, use synthetic data

Key changes:
```javascript
// Fetch external data
async function loadData() {
  try {
    const response = await fetch('/data/seismic-sample.json');
    const json = await response.json();
    return json.waveform;
  } catch (e) {
    console.warn('Failed to load external data, using synthetic data');
    return generateSyntheticData(); // fallback
  }
}
```

Also ensure D3 is imported correctly - the current code uses `import * as d3 from 'd3'` which should work.
  </action>
  <verify>
1. Build succeeds: `npm run build`
2. Page loads without console errors
3. Visualization renders with data
4. Works when embedded in blog post (MDX)
  </verify>
  <done>
InteractiveSeismogram loads data from external JSON file. If file is missing, falls back to synthetic data generation.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify blog post integration works</name>
  <files>src/content/blog/interactive-seismology.mdx</files>
  <action>
Verify that the interactive seismogram component renders correctly in the MDX blog post:

1. Review the current MDX file imports the component correctly
2. Ensure the component renders when the blog post is viewed
3. Test that the visualization works within the blog post context
4. Check that the component's scripts initialize properly in MDX context

The MDX already imports and uses `<InteractiveSeismogram />`. Verify:
- Astro's MDX integration handles the component correctly
- Client-side scripts execute in the MDX context
- CSS styles are applied correctly

If issues exist, fix the component or MDX file to ensure proper integration.
  </action>
  <verify>
1. Build succeeds: `npm run build`
2. Navigate to /blog/interactive-seismology
3. Interactive seismogram component renders
4. D3 visualization initializes and displays waveform
5. Zoom/pan interactions work
  </verify>
  <done>
Blog post at /blog/interactive-seismology displays working interactive seismogram visualization.
  </done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Interactive D3.js data visualizations with external data loading</what-built>
  <how-to-verify>
1. Visit https://ajamj.github.io/
2. Scroll to "Research Data Visualization" section
3. Verify the seismogram visualization is visible and interactive
4. Try zooming (mouse wheel) and panning (drag)
5. Visit https://ajamj.github.io/blog/interactive-seismology
6. Verify the same visualization works in the blog post
7. Check that hover shows time and amplitude values
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → static | Static site, data fetched from local static JSON |
| client → external | No external API calls (data is bundled/hosted) |

## STRIDE Threat Register

This is a read-only static site with minimal attack surface.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | seismic-sample.json | Accept | Public geophysics demo data, intentionally public |
| T-06-02 | Denial of Service | Large data file | Accept | Static JSON is small (~50KB), minimal risk |
</threat_model>

<verification>
- [ ] InteractiveSeismogram component renders on homepage
- [ ] External data loads from JSON file
- [ ] Zoom and pan interactions work
- [ ] Blog post integration functions correctly
- [ ] Build completes without errors
</verification>

<success_criteria>
1. D3.js interactive seismogram is embedded on the site
2. Visualization loads external seismic data from JSON file
3. User can interact with data (zoom, pan, hover)
4. Visualization works in blog post context
5. Site builds and deploys successfully
</success_criteria>

<output>
After completion, create `.planning/phases/06-data-viz/06-01-SUMMARY.md`
</output>