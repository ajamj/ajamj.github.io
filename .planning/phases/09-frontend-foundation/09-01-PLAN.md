---
phase: 09-frontend-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []
user_setup:
  - service: cloudflare
    why: "Deploy React frontend"
    dashboard_config:
      - task: "Create new Pages project"
        location: "Cloudflare Dashboard -> Workers & Pages -> Create application"
      - task: "Get Cloudflare account"
        location: "cloudflare.com"
---

<objective>
Set up React Vite project with routing and core components.
</objective>

<context>
@docs/superpowers/specs/2026-04-19-cloudflare-react-django-design.md
@.planning/phases/08-backend-api/08-01-PLAN.md

## Frontend Pages Required (from spec)
- `/` — Home (hero, intro, featured)
- `/about` — About/Bio
- `/projects` — Project portfolio
- `/cv` — Resume/CV
- `/blog` — Blog list
- `/blog/:slug` — Blog post

## Components Required
- Header — Navigation
- Footer — Links, contact
- Hero — Hero section
- ProjectCard — Project display
- BlogCard — Blog preview
- ContactForm — Contact submission
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize React Vite project</name>
  <files>frontend/</files>
  <action>
Create React Vite project:
```bash
npm create vite@latest frontend -- --template react
cd frontend
npm install
```

Directory structure:
```
frontend/
├── index.html
├── package.json
├── vite.config.js
├── src/
│   ├── main.jsx
│   ├── App.jsx
│   ├── App.css
│   ├── index.css
│   ├── components/
│   │   ├── Header.jsx
│   │   ├── Footer.jsx
│   │   ├── Hero.jsx
│   │   ├── ProjectCard.jsx
│   │   ├── BlogCard.jsx
│   │   └── ContactForm.jsx
│   ├── pages/
│   │   ├── Home.jsx
│   │   ├── About.jsx
│   │   ├── Projects.jsx
│   │   ├── CV.jsx
│   │   ├── Blog.jsx
│   │   └── BlogPost.jsx
│   └── styles/
│       └── global.css
```

Additional dependencies:
```bash
npm install react-router-dom
```
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>React Vite project builds without errors</done>
</task>

<task type="auto">
  <name>Task 2: Set up React Router</name>
  <files>frontend/src/main.jsx, frontend/src/App.jsx</files>
  <action>
Configure main.jsx with BrowserRouter:

```jsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import { BrowserRouter } from 'react-router-dom'
import App from './App.jsx'
import './index.css'

createRoot(document.getElementById('root')).render(
  <StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </StrictMode>,
)
```

Configure App.jsx with routes:

```jsx
import { Routes, Route } from 'react-router-dom'
import Header from './components/Header'
import Footer from './components/Footer'
import Home from './pages/Home'
import About from './pages/About'
import Projects from './pages/Projects'
import CV from './pages/CV'
import Blog from './pages/Blog'
import BlogPost from './pages/BlogPost'
import './App.css'

function App() {
  return (
    <div className="app">
      <Header />
      <main>
        <Routes>
          <Route path="/" element={<Home />} />
          <Route path="/about" element={<About />} />
          <Route path="/projects" element={<Projects />} />
          <Route path="/cv" element={<CV />} />
          <Route path="/blog" element={<Blog />} />
          <Route path="/blog/:slug" element={<BlogPost />} />
        </Routes>
      </main>
      <Footer />
    </div>
  )
}

export default App
```
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>Router configured, all routes build without errors</done>
</task>

<task type="auto">
  <name>Task 3: Create Header and Footer components</name>
  <files>frontend/src/components/Header.jsx, frontend/src/components/Footer.jsx</files>
  <action>
Create Header.jsx:

```jsx
import { Link, useLocation } from 'react-router-dom'
import './Header.css'

function Header() {
  const location = useLocation()
  
  const isActive = (path) => location.pathname === path ? 'active' : ''

  return (
    <header className="header">
      <div className="header-container">
        <Link to="/" className="logo">Ajam Jamaludin</Link>
        <nav className="nav">
          <Link to="/" className={isActive('/')}>Home</Link>
          <Link to="/about" className={isActive('/about')}>About</Link>
          <Link to="/projects" className={isActive('/projects')}>Projects</Link>
          <Link to="/cv" className={isActive('/cv')}>CV</Link>
          <Link to="/blog" className={isActive('/blog')}>Blog</Link>
        </nav>
      </div>
    </header>
  )
}

export default Header
```

Create Header.css:
```css
.header {
  padding: 1rem 2rem;
  border-bottom: 1px solid #eee;
}

.header-container {
  max-width: 1200px;
  margin: 0 auto;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.logo {
  font-size: 1.5rem;
  font-weight: bold;
  text-decoration: none;
  color: #333;
}

.nav {
  display: flex;
  gap: 1.5rem;
}

.nav a {
  text-decoration: none;
  color: #555;
  transition: color 0.2s;
}

.nav a:hover, .nav a.active {
  color: #000;
}
```

Create Footer.jsx:
```jsx
import './Footer.css'

function Footer() {
  return (
    <footer className="footer">
      <div className="footer-container">
        <p>&copy; 2026 Ajam Jamaludin. All rights reserved.</p>
        <div className="footer-links">
          <a href="https://github.com/ajamj" target="_blank" rel="noopener noreferrer">GitHub</a>
          <a href="https://linkedin.com/in/ajamj" target="_blank" rel="noopener noreferrer">LinkedIn</a>
          <a href="mailto:ajamj@email.com">Email</a>
        </div>
      </div>
    </footer>
  )
}

export default Footer
```
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | grep -E "(error|warning)" | head -10</automated>
  </verify>
  <done>Header and Footer components created and build without errors</done>
</task>

<task type="auto">
  <name>Task 4: Create basic page components</name>
  <files>frontend/src/pages/Home.jsx, frontend/src/pages/About.jsx, frontend/src/pages/Projects.jsx</files>
  <action>
Create Home.jsx:

```jsx
import Hero from '../components/Hero'
import { Link } from 'react-router-dom'

function Home() {
  return (
    <div className="home">
      <Hero />
      <section className="featured-section">
        <h2>Featured Work</h2>
        <p>Geophysics research and seismic data processing projects.</p>
        <Link to="/projects" className="btn">View Projects</Link>
      </section>
    </div>
  )
}

export default Home
```

Create About.jsx:

```jsx
function About() {
  return (
    <div className="about">
      <h1>About Me</h1>
      <div className="about-content">
        <p>
          I am a junior geophysics researcher specializing in seismology and seismic exploration.
          Currently working at Universitas Gadjah Mada, focusing on earthquake monitoring
          and seismic data processing.
        </p>
        <h2>Research Interests</h2>
        <ul>
          <li>Earthquake source mechanisms</li>
          <li>Seismic waveform analysis</li>
          <li>Tomographic imaging</li>
          <li>Seismic hazard assessment</li>
        </ul>
      </div>
    </div>
  )
}

export default About
```

Create Projects.jsx:

```jsx
import { useState, useEffect } from 'react'
import ProjectCard from '../components/ProjectCard'

function Projects() {
  const [projects, setProjects] = useState([])
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    // Will fetch from API in Phase 10
    setProjects([])
    setLoading(false)
  }, [])

  return (
    <div className="projects">
      <h1>Projects</h1>
      {loading ? (
        <p>Loading...</p>
      ) : projects.length === 0 ? (
        <p>No projects yet. Check back soon!</p>
      ) : (
        <div className="projects-grid">
          {projects.map(project => (
            <ProjectCard key={project.id} project={project} />
          ))}
        </div>
      )}
    </div>
  )
}

export default Projects
```
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | grep -E "error" | head -5</automated>
  </verify>
  <done>All page components created</done>
</task>

<task type="auto">
  <name>Task 5: Create remaining page components</name>
  <files>frontend/src/pages/CV.jsx, frontend/src/pages/Blog.jsx, frontend/src/pages/BlogPost.jsx</files>
  <action>
Create CV.jsx:

```jsx
function CV() {
  return (
    <div className="cv">
      <h1>Curriculum Vitae</h1>
      <div className="cv-section">
        <h2>Education</h2>
        <div className="cv-item">
          <h3>B.Sc. Geophysics</h3>
          <p>Universitas Gadjah Mada</p>
          <p className="date">2022 - 2026</p>
        </div>
      </div>
      <div className="cv-section">
        <h2>Experience</h2>
        <div className="cv-item">
          <h3>Research Assistant</h3>
          <p>Geophysics Laboratory, UGM</p>
          <p className="date">2024 - Present</p>
        </div>
      </div>
      <a href="/cv.pdf" className="btn" download>Download CV</a>
    </div>
  )
}

export default CV
```

Create Blog.jsx:

```jsx>
import { useState, useEffect } from 'react'
import { Link } from 'react-router-dom'
import BlogCard from '../components/BlogCard'

function Blog() {
  const [posts, setPosts] = useState([])
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    // Will fetch from API in Phase 10
    setPosts([])
    setLoading(false)
  }, [])

  return (
    <div className="blog">
      <h1>Blog</h1>
      {loading ? (
        <p>Loading...</p>
      ) : posts.length === 0 ? (
        <p>No posts yet. Check back soon!</p>
      ) : (
        <div className="blog-grid">
          {posts.map(post => (
            <BlogCard key={post.id} post={post} />
          ))}
        </div>
      )}
    </div>
  )
}

export default Blog
```

Create BlogPost.jsx:

```jsx
import { useState, useEffect, useParams } from 'react'

function BlogPost() {
  const { slug } = useParams()
  const [post, setPost] = useState(null)
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    // Will fetch from API in Phase 10
    setPost(null)
    setLoading(false)
  }, [slug])

  if (loading) return <div>Loading...</div>
  if (!post) return <div>Post not found</div>

  return (
    <div className="blog-post">
      <h1>{post.title}</h1>
      <p className="post-date">{post.date}</p>
      <div className="post-content">
        {/* Content rendered as HTML */}
        <div dangerouslySetInnerHTML={{ __html: post.content }} />
      </div>
    </div>
  )
}

export default BlogPost
```
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | grep -E "error" | head -5</automated>
  </verify>
  <done>All page components created</done>
</task>

<task type="auto">
  <name>Task 6: Configure Vite for Cloudflare Pages</name>
  <files>frontend/vite.config.js</files>
  <action>
Update vite.config.js for Cloudflare Pages:

```javascript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  build: {
    outDir: 'dist',
  },
  // For Cloudflare Pages SPA routing
  routes: {
    match: 'all',
  },
})
```

Create _headers file for Cloudflare:
```
/*
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
```

Create _redirects file for SPA:
```
/*    /index.html   200
```

Note: Cloudflare Pages supports SPA through _redirects file.
  </action>
  <verify>
    <animated>cd frontend && npm run build</animated>
  </verify>
  <done>Vite configured, builds successfully</done>
</task>

</tasks>

<verification>
1. React Vite project runs: `npm run dev`
2. All routes accessible (/, /about, /projects, /cv, /blog)
3. Build produces dist/ folder
</verification>

<success_criteria>
- React Vite project initialized
- React Router configured with all routes
- Header and Footer components created
- All 6 page components created
- Build works for Cloudflare Pages
</success_criteria>

<output>
After completion, create `.planning/phases/09-frontend-foundation/09-01-SUMMARY.md`
</output>