---
phase: 03-contact-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/layouts/Layout.astro, src/components/ContactForm.astro]
autonomous: true
requirements: [CONT-06, TECH-01, TECH-02, TECH-03]
must_haves:
  truths:
    - "Contact section displays email and social links clearly"
    - "HTML head contains comprehensive SEO meta tags (Title, Description, OpenGraph)"
    - "No tracking scripts or analytics present"
    - "Accessibility improved with proper semantic structure"
  artifacts:
    - path: "src/layouts/Layout.astro"
      provides: "Global SEO metadata"
      contains: ["meta property", "og:title", "og:description"]
    - path: "src/components/ContactForm.astro"
      provides: "Social links contact section"
  key_links:
    - from: "src/layouts/Layout.astro"
      to: "Astro.props"
      via: "SEO meta tags injection"
---

<objective>
Update SEO metadata and finalize the Contact section.

Purpose: Improve search discoverability and ensure visitors can easily connect.
Output: Enhanced SEO meta tags in `Layout.astro` and polished Contact section in `ContactForm.astro`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/layouts/Layout.astro
@src/components/ContactForm.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OpenGraph and SEO meta tags to Layout</name>
  <files>src/layouts/Layout.astro</files>
  <action>
    Add OpenGraph (`og:title`, `og:description`, `og:type`, `og:url`, `og:image`) and Twitter card tags to `Layout.astro`. 
    Ensure the `description` prop is passed dynamically to meta tags.
  </action>
  <verify>
    <automated>grep -q "og:title" src/layouts/Layout.astro</automated>
  </verify>
  <done>
    Meta tags added and verified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refine ContactForm accessibility and structure</name>
  <files>src/components/ContactForm.astro</files>
  <action>
    Ensure ContactForm accessibility by adding `aria-labelledby` or proper labels for social links. 
    Ensure no hidden analytics tags were accidentally added.
  </action>
  <verify>
    <automated>grep -q "aria-label" src/components/ContactForm.astro</automated>
  </verify>
  <done>
    ContactForm accessibility verified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Contact Form | Input validation at client-side (rely on Netlify for actual form submission security) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Contact Form | mitigate | Use Netlify honeypot / bot detection (built-in) |
| T-03-02 | Info Disclosure | Meta Tags | accept | No PII in meta tags, contact email is public |
</threat_model>

<verification>
Check site head for new SEO tags and verify contact section layout on mobile.
</verification>

<success_criteria>
SEO meta tags present; Contact section functional and accessible.
</success_criteria>
