---
phase: 11-d3-viz-port
plan: 01
type: execute
wave: 1
depends_on: [10-frontend-integration]
files_modified: [frontend/package.json, frontend/src/pages/Home.jsx]
autonomous: true
requirements: [REQ-11-01, REQ-11-02]
user_setup: []
---

<objective>
Port interactive seismic data visualizations from Astro components to React components using D3.js.
</objective>

<context>
The Astro site features two main D3 visualizations:
1. `InteractiveSeismogram.astro`: A synthetic seismic wave with zoom and coordinate display.
2. `EarthquakeViz.astro`: A magnitude vs. depth scatter plot with tooltips and depth-based coloring.

We need to recreate these in React while maintaining the "Digital Lithosphere" aesthetic.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install D3 dependency</name>
  <files>frontend/package.json</files>
  <action>
Install d3 in the frontend directory:
```bash
cd frontend && npm install d3
```
  </action>
  <verify>
    <automated>grep '"d3":' frontend/package.json</automated>
  </verify>
  <done>D3 installed and listed in dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Port SeismogramViz component</name>
  <files>frontend/src/components/SeismogramViz.jsx, frontend/src/components/SeismogramViz.css</files>
  <action>
Create `SeismogramViz.jsx` using `useRef` and `useEffect` for D3 lifecycle management. Implement:
- Synthetic data generation (P/S wave signal + noise).
- Line chart with `d3.line()`.
- Zoom behavior with `d3.zoom()`.
- Coordinate tracking on mouse move.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>Seismogram visualization functional in React</done>
</task>

<task type="auto">
  <name>Task 3: Port EarthquakeViz component</name>
  <files>frontend/src/components/EarthquakeViz.jsx, frontend/src/components/EarthquakeViz.css</files>
  <action>
Create `EarthquakeViz.jsx`. Implement:
- Scatter plot of magnitude vs. depth.
- Color coding based on depth thresholds (<20km, 20-40km, >40km).
- Interactive tooltips showing location and metadata.
- SVG responsiveness.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>Earthquake distribution plot functional in React</done>
</task>

<task type="auto">
  <name>Task 4: Create DataViz section</name>
  <files>frontend/src/components/DataVizSection.jsx</files>
  <action>
Create a wrapper section component that matches the layout of `ResearchDataViz.astro`, including the decorative HUD cards.
  </action>
  <verify>
    <automated>ls frontend/src/components/DataVizSection.jsx</automated>
  </verify>
  <done>DataViz section scaffolded</done>
</task>

<task type="auto">
  <name>Task 5: Integrate into Home page</name>
  <files>frontend/src/pages/Home.jsx</files>
  <action>
Import and render `DataVizSection` in `Home.jsx` below the Hero/Projects sections.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>Home page updated with visualizations</done>
</task>

</tasks>

<verification>
1. Seismogram wave renders and supports zooming.
2. Earthquake scatter plot shows all data points with correct colors.
3. Tooltips appear on hover for earthquake points.
4. Layout remains responsive on mobile/desktop.
</verification>

<success_criteria>
- D3 visualizations are fully interactive in the React frontend.
- Aesthetic consistency (glassmorphism, colors) is maintained.
- Performance is optimized (no memory leaks from D3 resize listeners).
</success_criteria>

<output>
After completion, create `.planning/phases/11-d3-viz-port/11-01-SUMMARY.md`
</output>