---
phase: 12-deployment
plan: 01
type: execute
wave: 1
depends_on: [11-d3-viz-port]
files_modified: [frontend/.env.production, backend/portfolio/settings.py, .github/workflows/deploy.yml]
autonomous: true
requirements: [REQ-12-01, REQ-12-02]
user_setup: [CLOUDFLARE_API_TOKEN, DATABASE_URL]
---

<objective>
Configure and execute the production deployment of the decoupled React (Frontend) and Django (Backend) application.
</objective>

<context>
The site is migrating from a static Astro site to a decoupled architecture. We need to:
1. Configure production environment variables.
2. Update GitHub Actions to handle both build processes.
3. Verify cross-origin resource sharing (CORS) for the production domain.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Production Env files</name>
  <files>frontend/.env.production, backend/.env.production</files>
  <action>
Create production environment templates. 
- Frontend: Set `VITE_API_URL` to the production backend domain.
- Backend: Set `DEBUG=False` and `ALLOWED_HOSTS`.
  </action>
  <verify>
    <automated>ls frontend/.env.production</automated>
  </verify>
  <done>Production environment configurations established</done>
</task>

<task type="auto">
  <name>Task 2: Configure CORS for Production</name>
  <files>backend/portfolio/settings.py</files>
  <action>
Update `CORS_ALLOWED_ORIGINS` in Django settings to include the production frontend domain (e.g., https://ajamj.github.io).
  </action>
  <verify>
    <automated>grep 'CORS_ALLOWED_ORIGINS' backend/portfolio/settings.py</automated>
  </verify>
  <done>CORS configured for production</done>
</task>

<task type="auto">
  <name>Task 3: Update CI/CD Workflow</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
Modify the existing deploy workflow to:
- Install dependencies for both frontend and backend.
- Build the React frontend.
- Run backend migrations.
- (Draft) Deploy frontend to GitHub Pages/Cloudflare and backend to selected provider.
  </action>
  <verify>
    <automated>git diff .github/workflows/deploy.yml</automated>
  </verify>
  <done>CI/CD pipeline updated for decoupled stack</done>
</task>

<task type="auto">
  <name>Task 4: Create Final Retrospective</name>
  <files>.planning/RETROSPECTIVE.md</files>
  <action>
Document the migration journey from Astro to React/Django, covering architectural decisions and lessons learned.
  </action>
  <verify>
    <automated>ls .planning/RETROSPECTIVE.md</automated>
  </verify>
  <done>Project retrospective completed</done>
</task>

</tasks>

<verification>
1. Frontend build completes with production environment variables.
2. Backend settings verify `DEBUG=False` for safety.
3. Workflow YAML is syntactically valid.
</verification>

<success_criteria>
- The project is ready for one-click deployment via GitHub Actions.
- Documentation for the new decoupled architecture is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/12-deployment/12-01-SUMMARY.md` and officially close the project migration.
</output>